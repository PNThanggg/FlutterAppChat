name: chat_model
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  chat_translation:
    path: ../chat_translation
  chat_platform:
    path: ../chat_platform
  chat_config:
    path: ../chat_config

  enum_to_string: ^2.2.0
  chopper: ^8.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

